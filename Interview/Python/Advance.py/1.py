What is the difference between the `is` and `==` operators in Python?

# The `is` operator checks for identity, meaning it checks if two variables point to the same object in memory.
# The `==` operator checks for equality, meaning it checks if the values of two variables are equal.

What is context manager in Python?
# A context manager in Python is a construct that allows you to allocate and release resources precisely when you want to.
# It is typically used with the `with` statement to ensure that resources are properly managed, such as opening and closing files,
# acquiring and releasing locks, or connecting and disconnecting from databases.
# The context manager handles the setup and teardown of resources automatically, ensuring that they are properly cleaned up even if an error occurs.
#