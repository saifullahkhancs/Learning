static class Solution {
    static int RemoveDuplicates(int[] ref nums)
     {
        // var len = length(nums);

        // while 
        foreach(int num in nums)
        {
            for()
            
        }

        
    }
}
int[] nums = [0,0,1,1,1,2,2,3,3,4]
Solution.RemoveDuplicates(ref nums)