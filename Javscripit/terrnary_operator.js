// The ternary operator, also known as the conditional operator, is a 
// concise way to write conditional (if-else) statements in JavaScript 
// and many other programming languages. It takes the form:

// Syntaxxx

   //  **   condition ? expression_if_true : expression_if_false;   ** //

   const  age =  20;

   const canvote = (age => 18) ? "Yes": "No";
   console.log(canvote);

    var friend = 0;

   console.log((friend>0 ? "have friends": "No Friends"   ));